// Albums { name: tracks: songs: [ { name:, artist: }}}

const AlbumSchema= new Schema({

name: String,

tracks: Number,

songs:

[SongListSchemal

const SongListSchema= new Schema({

artist: String,

title: String
